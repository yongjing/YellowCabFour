[project]
name = "yellowcab-flask"
version = "0.1.0"
description = ""
authors = [
    {name = "Aboubakar Koita",email = "makalou.bouba@pm.me"}
]
readme = "README.md"
requires-python = ">=3.10,<3.11"
dependencies = [
    "flask (>=3.1.0,<4.0.0)",
    "loguru (>=0.7.3,<0.8.0)"
]

[tool.poetry]
packages = [{include = "yellowcab_flask", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
